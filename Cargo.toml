[package]
name = "paper-size"
version = "0.1.0"
edition = "2024"
description = "Standard paper sizes (ISO 216, JIS, ANSI, North American) with parsing and unit conversion"
license = "MIT"
repository = "https://github.com/unstabler/paper-size"
keywords = ["paper", "page-size", "iso216", "print", "pdf"]
categories = ["data-structures", "no-std"]
rust-version = "1.85.0"

[features]
default = ["std"]
std = []
